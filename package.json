{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "env mocha --recursive -r ts-node/register 'tst/**/*.ts'",
    "build": "npm run build-client && tsc",
    "dev": "npm run build && nodemon -r source-map-support/register dist/index.js",
    "start": "npm run build && node -r source-map-support/register dist/index.js",
    "build-client": "mkdir -p dist/static/ && cd ./src/static/ && cp -r ./html/* ../../dist/static/ && cp -r ./css/* ../../dist/static/ && tsc",
    "clean": "rm -r dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/body-parser": "^1.19.5",
    "@types/chai": "^5.0.0",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.0",
    "@types/mocha": "^10.0.9",
    "@types/mongodb": "^4.0.6",
    "@types/nodemailer": "^6.4.16",
    "chai": "^5.1.1",
    "mocha": "^10.7.3",
    "nodemon": "^3.1.7",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "googleapis": "^144.0.0",
    "mongodb": "^6.9.0",
    "nodemailer": "^6.9.15"
  }
}
